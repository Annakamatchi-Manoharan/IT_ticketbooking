# Security Review
- Zod validation on API inputs.
- Parameterized SQL prevents SQL injection.
- Helmet, CORS, rate limiting enabled.
- File upload size and MIME allow-list enforced.
- JWT auth + role checks for access control.
- Audit logging for auth and ticket events.

